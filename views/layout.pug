doctype html
html
    head
        title Medicalstore
        link(rel='stylesheet', href='/bootstrap/css/bootstrap.min.css')
        link(rel='stylesheet', href='/css/style.css')
    body
        .container
            nav.navbar.navbar-expand-md.navbar-dark.bg-dark.fixed-top
                a.navbar-brand(href='#') Medicalstore
                button.navbar-toggler(type='button', data-toggle='collapse', data-target='#navbar', aria-controls='navbar', aria-expanded='false', aria-label='Toggle navigation')
                    span.navbar-toggler-icon
                #navbar.collapse.navbar-collapse
                    ul.navbar-nav.mr-auto
                        li.nav-item.active
                            a.nav-link(href='/home') Home
                            span.sr-only (current)
                        li.nav-item
                            a.nav-link(href='/medicine/list') Medicine
                            span.sr-only (current)
                        li.nav-item
                            a.nav-link(href='/medicine/add') Add Medicine
                           
                    ul.navbar-nav.ms-auto
                        li.nav-item
                            form(action="/medicine/search/:key" method="GET")
                                input(type='text' placeholder='search' name='key')
                                input(type='submit' value='search')
                        li.nav-item
                            a.nav-link(href='/user/logout') LOGOUT      
                    //-span#currentDateTime.navbar-text 
        .container  
            br
            br
            br
            br
            br
            block content
        script(src='https://code.jquery.com/jquery-3.5.1.min.js')
        script(src='/bootstrap/js/bootstrap.js')
        script(src='/js/main.js')
        script.
            function updateDateTime() {
                const dateTimeElement = document.getElementById("currentDateTime").innerHTML;
                const now = new Date();
                const options = {
                weekday: "long",
                year: "numeric",
                month: "long",
                day: "numeric",
                hour: "numeric",
                minute: "numeric",
                second: "numeric",
                };
                const formattedDateTime = now.toLocaleDateString(undefined, options);
                dateTimeElement.textContent = formattedDateTime;
            }
            ;
            updateDateTime(); // Update immediately
            setInterval(updateDateTime, 1000); // Update every second
